#include <stdio.h>

/**
 * @brief Initialize the Hyperliquid SBE adapter.
 *
 * Placeholder for future connection setup (WebSocket handshake,
 * SBE schema negotiation, feed subscription).
 */
void hl_init_adapter() {
    printf("[*] Hyperliquid SBE Adapter Initialized.\n");
}
